const utils = {
  getProjectName: require("./get-project-name"),
  getProjectRootPath: require("./get-project-root-path"),
  decorateRange: require("./decorate-range"),
  extractContent: require("./extract-content"),
};

export default utils;
